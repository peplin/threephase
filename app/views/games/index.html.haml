- content_for :left_column do
  Pick a game, please.

- content_for :right_column do
  - if current_user
    %a{:href => new_game_url}New Game

  %ul
    - @games.each do |game|
      %li
        %a{:href => game_path(game)}= game
