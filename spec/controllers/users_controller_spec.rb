require 'spec_helper'

describe UsersController do
  before :all do
    @model = User
  end

  before :all do
    @user = Factory :user
  end

  context "as an admin" do
  end
  #TODO lots of work here
end
